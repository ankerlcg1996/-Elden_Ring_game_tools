<UserControl x:Class="TarnishedTool.Views.Tabs.SettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
             Focusable="True"
             FocusManager.FocusedElement="{x:Null}"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <DataTemplate x:Key="HotkeyItemTemplate">
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="{Binding DisplayName}"
                           Width="150"
                           VerticalAlignment="Center" />
                <TextBox Text="{Binding HotkeyText}"

                         Style="{StaticResource HotkeyTextBoxStyle}"
                         Tag="{Binding ActionId}"
                         GotFocus="HotkeyTextBox_GotFocus"
                         LostFocus="HotkeyTextBox_LostFocus"
                         KeyDown="HotkeyTextBox_KeyDown" />
            </StackPanel>
        </DataTemplate>
        <xcad:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility" />
        <xcad:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>
    <Border BorderThickness="1" BorderBrush="#333" CornerRadius="3" Margin="5">
        <StackPanel Margin="10">
            <TextBlock Text="选项" FontWeight="Bold" Margin="0,0,0,10" />

            <StackPanel Margin="0,5,0,10">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <CheckBox Content="隐藏 Logo"
                              IsChecked="{Binding IsNoLogoEnabled}"
                              Margin="0,0,10,0" />
                    <CheckBox Content="窗口置顶"
                              IsChecked="{Binding IsAlwaysOnTopEnabled}"
                              Margin="0,0,10,0" />
                    <CheckBox Content="静音音乐"
                              IsChecked="{Binding IsMuteMusicEnabled}"
                              Margin="0,0,0,0" />
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Content="热键提醒"
                                  IsChecked="{Binding IsHotkeyReminderEnabled}"
                                  Margin="10 ,0,0,0" />
                        <Border
                            Background="#3498db"
                            CornerRadius="10"
                            Width="12"
                            Height="12"
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            ToolTip="新开周目且热键仍启用时显示弹窗提醒"
                            ToolTipService.InitialShowDelay='100'>
                            <TextBlock Text="i"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       FontSize="10" />
                        </Border>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <CheckBox Content="卡顿修复"
                              IsChecked="{Binding IsStutterFixEnabled}"
                              Margin="0,0,10,0" />
                    <CheckBox Content="禁用成就"
                              IsChecked="{Binding IsDisableAchievementsEnabled}"
                              Margin="0,0,0,0" />
                </StackPanel>
            </StackPanel>

            <Separator Margin="0,0,0,10" />

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="热键绑定" FontWeight="Bold" Margin="10,0,0,10" HorizontalAlignment="Left" />
                <CheckBox Content="启用热键"
                          IsChecked="{Binding IsEnableHotkeysEnabled}"
                          Margin="40,0,0,10" />
                <CheckBox Content="屏蔽游戏原始按键动作"
                          IsChecked="{Binding IsBlockGameHotkeysEnabled}"
                          Margin="20,0,10,10" />

                <Border Background="#3498db" CornerRadius="10" Width="12" Height="12" Margin="5,0,0,10"
                        VerticalAlignment="Center"
                        ToolTip="按下此处绑定热键时阻止游戏原动作。&#x0A;根据配置可能有少量延迟"
                        ToolTipService.InitialShowDelay="100">
                    <TextBlock Text="i" Foreground="White" HorizontalAlignment="Center"
                               VerticalAlignment="Center" FontWeight="Bold" FontSize="10" />
                </Border>
                <Button
                    Content="清除全部绑定"
                    Margin="15,0,0,10"
                    Command="{Binding ClearHotkeysCommand}" />
            </StackPanel>


            <Grid Margin="0,5,0,0">

                <TabControl Margin="0,5,0,0"
                            Visibility="{Binding Hotkeys.IsSearchActive, Converter={StaticResource InverseBoolToVisibility}}">

                    <TabItem Header="玩家" Width="56">
                        <ScrollViewer MaxHeight="430" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Player]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="传送" Width="54">
                        <ScrollViewer MaxHeight="430" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Travel]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="敌人" Width="67">
                        <ScrollViewer MaxHeight="430" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Enemies]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="目标" Width="56">
                        <ScrollViewer MaxHeight="430" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Target]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="实用" Width="54">
                        <ScrollViewer MaxHeight="430" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Utility]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="事件" Width="53">
                        <ScrollViewer MaxHeight="430" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Event]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="物品" Width="52">
                        <ScrollViewer MaxHeight="430" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Items]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="高级" Width="76">
                        <ScrollViewer MaxHeight="430" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Advanced]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>

                <ScrollViewer MaxHeight="390"
                              Margin="0,40,0,0"
                              VerticalScrollBarVisibility="Auto"
                              Visibility="{Binding Hotkeys.IsSearchActive, Converter={StaticResource BoolToVisibility}}">
                    <ItemsControl ItemsSource="{Binding Hotkeys.Items}"
                                  Padding="10"
                                  ItemTemplate="{StaticResource HotkeyItemTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
                <TextBox Text="{Binding Hotkeys.SearchText, UpdateSourceTrigger=PropertyChanged}"
                         HorizontalAlignment="Right"
                         VerticalAlignment="Top"
                         Width="90"
                         Height="27"
                         Margin="0,7,5,0">
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                                  Focusable="false"
                                                  HorizontalScrollBarVisibility="Hidden"
                                                  VerticalScrollBarVisibility="Hidden"
                                                  VerticalAlignment="Center"
                                                  Margin="5,0,0,0" />
                                    <TextBlock x:Name="placeholderText"
                                               Text="搜索..."
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Left"
                                               Margin="5,0,0,0"
                                               Foreground="Gray"
                                               IsHitTestVisible="False">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition
                                                                Binding="{Binding Path=Text, RelativeSource={RelativeSource AncestorType=TextBox}}"
                                                                Value="" />
                                                            <Condition
                                                                Binding="{Binding Path=IsKeyboardFocused, RelativeSource={RelativeSource AncestorType=TextBox}}"
                                                                Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush"
                                            Value="{StaticResource AccentBrush}" />
                                    <Setter TargetName="border" Property="BorderThickness" Value="1.5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>


