<windows:TopmostWindow x:Class="TarnishedTool.Views.Windows.EventLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:controls="clr-namespace:TarnishedTool.Controls"
        xmlns:converters="clr-namespace:TarnishedTool.Converters"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:windows="clr-namespace:TarnishedTool.Views.Windows"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Title="事件日志窗口" Height="450" Width="600"
        Style="{StaticResource DefaultWindowStyle}">

    <windows:TopmostWindow.Resources>
        <converters:LogEntryToBrushConverter x:Key="LogBrushConverter" />
    </windows:TopmostWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:TitleBar Title="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" />


        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="350" />
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0"
                     x:Name="LogListBox"
                     SelectionMode="Extended"
                     ItemsSource="{Binding LogEntries}"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     ScrollViewer.ScrollChanged="LogListBox_ScrollChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding DisplayText}"
                                   Foreground="{Binding Converter={StaticResource LogBrushConverter}}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <EventSetter Event="MouseDoubleClick" Handler="Event_DoubleClick" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.InputBindings>
                    <KeyBinding Command="ApplicationCommands.SelectAll"
                                Modifiers="Ctrl"
                                Key="A" />
                </ListBox.InputBindings>  
            </ListBox>

            <Grid Grid.Column="1">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="10, 10, 0, 0"
                    Text="日志选项" />
                
                <CheckBox  Grid.Row="0"
                           Grid.Column="1" 
                           x:Name="AlwaysOnTopCheckBox"
                           HorizontalAlignment="Right"
                           Margin="0,0,10,0"
                           Checked="AlwaysOnTopCheckBox_Checked"
                           Unchecked="AlwaysOnTopCheckBox_Unchecked">
                    Always on Top
                </CheckBox>

                <CheckBox
                    Grid.Row="1"
                    Grid.Column="0"
                    Margin="10, 10, 0, 0"
                    Content="隐藏重复 ID"
                    IsChecked="{Binding IsLogUniqueIdsEnabled}" />

                <StackPanel Grid.Row="1"
                            Grid.Column="1"
                            Margin="10, 10, 0, 0"
                            Orientation="Horizontal">
                    <xctk:IntegerUpDown
                        Value="{Binding LogLimit, UpdateSourceTrigger=PropertyChanged}"
                        Width="80"
                        Maximum="20000" />

                    <TextBlock Text="日志上限" VerticalAlignment="Center"
                               Margin="10, 0, 0, 0">
                    </TextBlock>
                </StackPanel>

                <Button
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="10, 10, 0, 0"
                    Content="清空日志"
                    Command="{Binding ClearCommand}" />

                <Button Grid.Row="2"
                        Grid.Column="1"
                        VerticalAlignment="Bottom"
                        Command="{Binding ClearUniqueCommand}"
                        Margin="10, 10, 0, 0"
                        Content="清空唯一 ID" />

                <Grid Grid.Row="3"
                      Grid.Column="0">
                    <TextBox
                        Text="{Binding EventIdToExclude, UpdateSourceTrigger=PropertyChanged}"
                        VerticalAlignment="Center"
                        Height="30"
                        Width="150" FontSize="12" Margin="10, 10, 0, 0"
                        MaxLength="10"
                        x:Name="ExcludeEventBox" />
                    <TextBlock IsHitTestVisible="False" Text="输入要排除的事件" VerticalAlignment="Center"
                               HorizontalAlignment="Left" Margin="25,10,0,0" Foreground="DarkGray">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=ExcludeEventBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                </Grid>

                <Button Grid.Row="3"
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Margin="10, 10, 0, 0"

                        Command="{Binding AddToExcludeCommand}"
                        Content="加入排除" />

                <Button Grid.Row="4"
                        Grid.Column="0"
                        Content="导出排除 ID"
                        Command="{Binding ExportExcludedCommand}"
                        Margin="10, 10, 0, 0" />

                <Button Grid.Row="4"
                        Grid.Column="1"
                        Command="{Binding ImportExcludedCommand}"
                        Margin="10, 10, 0, 0"
                        Content="导入排除 ID" />

                <TextBlock Grid.Row="5"
                           Grid.Column="0"
                           Margin="10, 10, 0, 0"
                           Text="排除的事件 ID" />

                <ListBox
                    Margin="10"
                    Grid.Row="6"
                    Grid.Column="0"
                    MaxHeight="200"
                    ItemsSource="{Binding ExcludedEventIds}"
                    SelectedItem="{Binding SelectedExcludedId}"
                    Height="200">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <EventSetter Event="MouseDoubleClick" Handler="ExcludedEvent_DoubleClick" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>


                <Grid Grid.Row="6"
                      Grid.Column="1">
                    <StackPanel VerticalAlignment="Top" Margin="10, 10, 0, 0">
                        
                        <Button
                            Content="清空全部排除 ID"
                            Command="{Binding ClearExcludedCommand}" />
                        <Button
                            Margin="0, 10, 0, 0"
                            Content="将选中项加入排除"
                            Click="ExcludeSelected_Click" />
                        <Button
                            Margin="0, 10, 0, 0"
                            Content="从排除中移除 ID"
                            Command="{Binding RemoveFromExcludedCommand}" />
                    </StackPanel>
                    
                    <Button
                        Height="35"
                        VerticalAlignment="Bottom"
                        Margin="10, 10, 0, 0"
                        Content="{Binding PauseResumeText}"
                        Command="{Binding PauseResumeCommand}" />

                </Grid>
            </Grid>
        </Grid>
    </Grid>
</windows:TopmostWindow>


