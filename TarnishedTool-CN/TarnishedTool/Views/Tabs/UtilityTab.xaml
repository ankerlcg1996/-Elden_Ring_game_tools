<UserControl x:Class="TarnishedTool.Views.Tabs.UtilityTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:converters="clr-namespace:TarnishedTool.Converters"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="600">

    <UserControl.Resources>
        <converters:BooleanAndConverter x:Key="BooleanAndConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanOrConverter x:Key="BooleanOrConverter" />
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderThickness="1"
                BorderBrush="#333"
                CornerRadius="3"
                Margin="0,0,0,5">
            <StackPanel Margin="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="通用"
                               Grid.Column="0" />
                </Grid>

                <StackPanel Margin="0,5,0,0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,3" VerticalAlignment="Center">
                        <CheckBox Content="战斗中可开地图"
                                  IsChecked="{Binding IsCombatMapEnabled}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,20,0" />
                        <CheckBox Content="地牢可传送"
                                  IsChecked="{Binding IsDungeonWarpEnabled}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,20,0" />
                        <Button Content="强制保存"
                                IsEnabled="{Binding AreOptionsEnabled}"
                                Padding="5,3"
                                Command="{Binding SaveCommand}"
                                Margin="0,0,20,0" />

                        <Button Content="进入下一周目"
                                IsEnabled="{Binding AreOptionsEnabled}"
                                Padding="5,3"
                                Command="{Binding TriggerNgCycleCommand}"
                                Margin="0,0,0,0" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,10,0,0">
                        <xctk:DoubleUpDown
                            Value="{Binding GameSpeed}"
                            TextAlignment="Center"
                            Width="80"
                            Minimum="0.50"
                            Maximum="5"
                            Increment="0.50"
                            FormatString="F2"
                            BorderBrush="#3F3F46" />
                        <TextBlock Text="游戏速度" VerticalAlignment="Center" Margin="10,0,30,0" />
                        <CheckBox Content="记忆速度"
                                  IsChecked="{Binding IsRememberSpeedEnabled}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0" />
                        <Border
                            Background="#3498db"
                            CornerRadius="10"
                            Width="12"
                            Height="12"
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            ToolTip="重启应用后保留速度设置"
                            ToolTipService.InitialShowDelay='100'>
                            <TextBlock Text="i"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       FontSize="10" />
                        </Border>
                        <CheckBox Content="冻结世界"
                                  IsChecked="{Binding IsFreezeWorldEnabled}"
                                  Margin="20,0,0,0"
                                  VerticalAlignment="Center"
                                  IsEnabled="{Binding AreOptionsEnabled}" />

                        <Border
                            Background="#3498db"
                            CornerRadius="10"
                            Width="12"
                            Height="12"
                            Margin="5,0,0,0"
                            VerticalAlignment="Center"
                            ToolTip="按三角键逐帧前进"
                            ToolTipService.InitialShowDelay='100'>
                            <TextBlock Text="i"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontWeight="Bold"
                                       FontSize="10" />
                        </Border>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <CheckBox Content="100% 掉落"
                                  IsChecked="{Binding IsGuaranteedDropEnabled}"
                                  VerticalAlignment="Center" />
                        
                        <TextBlock Margin="15,0,0,0" Text="设置 FPS 上限"></TextBlock>
                        <xctk:IntegerUpDown
                            Margin="5,0,0,0"
                            Value="{Binding Fps}"
                            TextAlignment="Center"
                            Width="100"
                            Minimum="1"
                            Maximum="1000"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            BorderBrush="#3F3F46"
                            ></xctk:IntegerUpDown>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <TabControl Grid.Row="2" Height="170">
            <TabItem Header="穿模 / 相机">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            BorderThickness="1"
                            BorderBrush="#333"
                            CornerRadius="3"
                            Margin="0,0,10,0"
                            Padding="10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox Content="穿模"
                                          IsChecked="{Binding IsNoClipEnabled}"
                                          IsEnabled="{Binding AreOptionsEnabled}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,5,0" />
                                <Border
                                    Background="#3498db"
                                    CornerRadius="10"
                                    Width="12"
                                    Height="12"
                                    Margin="5,0,0,0"
                                    VerticalAlignment="Center"
                                    ToolTip="垂直移动：键盘 Ctrl/Space 或手柄 L2/R2"
                                    ToolTipService.InitialShowDelay="100">
                                    <TextBlock Text="i"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               FontWeight="Bold"
                                               FontSize="10" />
                                </Border>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <xctk:DoubleUpDown
                                    Value="{Binding NoClipSpeed}"
                                    TextAlignment="Center"
                                    Width="80"
                                    Minimum="0.5"
                                    Maximum="5.0"
                                    Increment="0.50"
                                    FormatString="F2"
                                    IsEnabled="{Binding IsNoClipEnabled}"
                                    BorderBrush="#3F3F46" />
                                <TextBlock Text="速度" VerticalAlignment="Center" Margin="5,0,0,0" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <CheckBox Content="禁用键盘垂直移动"
                                          IsChecked="{Binding IsNoClipKeyboardDisableEnabled}"
                                          IsEnabled="{Binding AreOptionsEnabled}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,5,0" />
                                <Border
                                    Background="#3498db"
                                    CornerRadius="10"
                                    Width="12"
                                    Height="12"
                                    Margin="5,0,0,0"
                                    VerticalAlignment="Center"
                                    ToolTip="可禁用键盘 CTRL/Space 的垂直移动"
                                    ToolTipService.InitialShowDelay="100">
                                    <TextBlock Text="i"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               FontWeight="Bold"
                                               FontSize="10" />
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1"
                            BorderThickness="1"
                            BorderBrush="#333"
                            CornerRadius="3"
                            Padding="10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <CheckBox Content="自由相机"
                                          IsChecked="{Binding IsFreeCamEnabled}"
                                          IsEnabled="{Binding AreOptionsEnabled}"
                                          VerticalAlignment="Center" />
                                <Border
                                    Background="#3498db"
                                    CornerRadius="10"
                                    Width="12"
                                    Height="12"
                                    Margin="5,0,10,0"
                                    VerticalAlignment="Center"
                                    ToolTip="当前仅支持手柄"
                                    ToolTipService.InitialShowDelay="100">
                                    <TextBlock Text="i"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               FontWeight="Bold"
                                               FontSize="10" />
                                </Border>
                            </StackPanel>

                            <CheckBox Content="玩家移动"
                                      IsChecked="{Binding IsPlayerMovementEnabled}"
                                      Margin="0,0,0,10"
                                      VerticalAlignment="Center">
                                <CheckBox.IsEnabled>
                                    <MultiBinding Converter="{StaticResource BooleanAndConverter}">
                                        <Binding Path="AreOptionsEnabled" />
                                        <Binding Path="IsFreeCamEnabled" />
                                    </MultiBinding>
                                </CheckBox.IsEnabled>
                            </CheckBox>

                            <Button Content="将自由相机移到玩家"
                                    Command="{Binding MoveCamToPlayerCommand}"
                                    IsEnabled="{Binding IsFreeCamEnabled}"
                                    Padding="5,0"
                                    Margin="0,0,0,5"
                                    HorizontalAlignment="Left" />
                            <Button Content="将玩家移到自由相机"
                                    Command="{Binding MovePlayerToCamCommand}"
                                    IsEnabled="{Binding IsFreeCamEnabled}"
                                    Padding="5,0"
                                    HorizontalAlignment="Left" />
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>


        <Border Grid.Row="3"
                BorderThickness="1"
                BorderBrush="#333"
                CornerRadius="3"
                Margin="0,0,0,5">
            <StackPanel Margin="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="渲染"
                               Grid.Column="0" />
                </Grid>

                <Grid Margin="10,5,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <CheckBox Content="显示碰撞箱"
                              IsChecked="{Binding IsDrawHitboxEnabled}"
                              Grid.Column="0" Grid.Row="0"
                              Margin="0,0,5,0" />

                    <CheckBox Content="玩家声音"
                              IsChecked="{Binding IsDrawPlayerSoundEnabled}"
                              Grid.Column="0" Grid.Row="1"
                              Margin="0,2,5,2" />
                    <CheckBox Content="布娃娃"
                              IsChecked="{Binding IsDrawRagdollsEnabled}"
                              Grid.Column="0" Grid.Row="2"
                              Margin="0,0,5,0" />
                    <CheckBox Content="削韧条"
                              IsChecked="{Binding IsDrawPoiseBarsEnabled}"
                              Grid.Column="0" Grid.Row="3"
                              Margin="0,0,5,0" />

                    <CheckBox Content="显示低位命中"
                              IsChecked="{Binding IsDrawLowHitEnabled}"
                              Grid.Column="1" Grid.Row="0"
                              Margin="5,2" />

                    <CheckBox Content="显示高位命中"
                              IsChecked="{Binding IsDrawHighHitEnabled}"
                              Grid.Column="1" Grid.Row="1"
                              Margin="5,2,5,2" />
                    <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal"
                                Margin="10,2,5,0">
                        <TextBlock Text="模式：" Margin="0,0,10,0" VerticalAlignment="Center" />
                        <xctk:IntegerUpDown Width="50" Height="20"
                                            Value="{Binding ColDrawMode, UpdateSourceTrigger=PropertyChanged}"
                                            Minimum="0"
                                            Maximum="5"
                                            VerticalAlignment="Center">
                            <xctk:IntegerUpDown.IsEnabled>
                                <MultiBinding Converter="{StaticResource BooleanOrConverter}">
                                    <Binding Path="IsDrawLowHitEnabled" />
                                    <Binding Path="IsDrawHighHitEnabled" />
                                </MultiBinding>
                            </xctk:IntegerUpDown.IsEnabled>
                        </xctk:IntegerUpDown>
                    </StackPanel>

                    <CheckBox Content="显示地图瓦片 1"
                              IsChecked="{Binding IsDrawMapTiles1Enabled}"
                              Grid.Column="2" Grid.Row="0"
                              Margin="5, 0, 0,2" />

                    <CheckBox Content="显示地图瓦片 2"
                              IsChecked="{Binding IsDrawMapTiles2Enabled}"
                              Grid.Column="2" Grid.Row="1"
                              Margin="5, 0, 0,2" />

                    <CheckBox Content="显示小地图"
                              IsChecked="{Binding IsDrawMiniMapEnabled}"
                              Grid.Column="2" Grid.Row="2"
                              Margin="5, 0, 0,2" />
                    <CheckBox Content="在世界地图显示瓦片"
                              IsChecked="{Binding IsDrawTilesOnMapEnabled}"
                              Grid.Column="2" Grid.Row="3"
                              Margin="5, 0, 0,2" />


                    <CheckBox Content="隐藏地图"
                              IsChecked="{Binding IsHideMapEnabled}"
                              Grid.Column="3" Grid.Row="0"
                              Margin="5, 0, 0,2" />

                    <CheckBox Content="隐藏角色"
                              IsChecked="{Binding IsHideCharactersEnabled}"
                              Grid.Column="3" Grid.Row="1"
                              Margin="5, 0, 0,2" />

                </Grid>
            </StackPanel>
        </Border>
        <Border Grid.Row="4"
                BorderThickness="1"
                BorderBrush="#333"
                CornerRadius="3"
                Margin="0,0,0,5">
            <TabControl Padding="10">
                <TabItem Header="菜单">
                    <WrapPanel>
                        <Button
                            Content="升级"
                            Command="{Binding OpenLevelUpCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,0,10,0" />
                        <Button
                            Content="分配圣杯瓶"
                            Command="{Binding OpenAllotCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,0,10,0" />
                        <Button
                            Content="记忆魔法"
                            Command="{Binding AttunementCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,0,10,0" />
                        <Button
                            Content="调配灵药"
                            Command="{Binding OpenPhysickCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,0,10,0" />
                        <Button
                            Content="打开宝箱"
                            Command="{Binding OpenChestCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,0,10,0" />
                        <Button
                            Content="大卢恩"
                            Command="{Binding OpenGreatRunesCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,0,0,0" />
                        <Button
                            Content="战灰"
                            Command="{Binding OpenAowCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,10,10,0" />
                        <Button
                            Content="修改服装"
                            Command="{Binding OpenAlterGarmentsCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,10,10,0" />
                        <Button
                            Content="强化"
                            Command="{Binding OpenUpgradeCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,10,10,0" />
                        <Button
                            Content="出售"
                            Command="{Binding OpenSellCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,10,10,0" />
                        <Button
                            Content="重生"
                            Command="{Binding OpenRebirthCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,10,10,0" />
                        <Button
                            Content="强化圣杯瓶"
                            Command="{Binding UpgradeFlaskCommand}"
                            Margin="0,10,10,0">
                            <Button.IsEnabled>
                                <MultiBinding Converter="{StaticResource BooleanAndConverter}">
                                    <Binding Path="AreOptionsEnabled" />
                                    <Binding Path="IsUpgradingFlask"
                                             Converter="{StaticResource InverseBooleanConverter}" />
                                </MultiBinding>
                            </Button.IsEnabled>
                        </Button>

                        <Button
                            Content="增加圣杯瓶次数"
                            Command="{Binding IncreaseChargesCommand}"
                            Margin="0,10,10,0">
                            <Button.IsEnabled>
                                <MultiBinding Converter="{StaticResource BooleanAndConverter}">
                                    <Binding Path="AreOptionsEnabled" />
                                    <Binding Path="IsIncreasingCharges"
                                             Converter="{StaticResource InverseBooleanConverter}" />
                                </MultiBinding>
                            </Button.IsEnabled>
                        </Button>
                        <Button
                            Content="打开镜子"
                            Command="{Binding OpenMirrorCommand}"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            Margin="0,10,10,0"/>
                        <CheckBox
                            Content="强化无消耗"
                            IsEnabled="{Binding AreOptionsEnabled}"
                            IsChecked="{Binding IsNoUpgradeCostEnabled}"
                            Margin="0,10,10,0"/>
                    </WrapPanel>
                </TabItem>

                <TabItem Header="商店">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="打开商店"
                                Command="{Binding OpenShopSelectorCommand}" />

                        <CheckBox
                            Content="显示完整商店列表"
                            IsChecked="{Binding IsShowFullShopLineupEnabled}"
                            Margin="15,0,0,0" />
                    </StackPanel>
                </TabItem>

            </TabControl>
        </Border>
    </Grid>

</UserControl>



